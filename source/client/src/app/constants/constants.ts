import { DOMAIN } from "./ignoreConstant";

export const BASE_URL = `http://${DOMAIN}:5000`;
export const IDLE = "IDLE";

export const Type = {
  SUCCESS: "SUCCESS",
  FAILURE: "FAILURE",
  DEFAULT: "DEFAULT",
};
